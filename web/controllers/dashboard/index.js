const query = require('./action/query')
const queryRange = require('./action/queryRange')
const dashboardConfig = require('./action/dashboardConfig')
const labels = require('./action/labels')

module.exports = {
  query,
  queryRange,
  dashboardConfig,
  labels
}
